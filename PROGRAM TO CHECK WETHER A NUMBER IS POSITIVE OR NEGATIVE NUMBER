//PROGRAM TO CHECK WETHER A NUMBER IS POSITIVE OR NEGATIVE NUMBER.
#include <stdio.h>
int main()
{
	int n;
	printf(" Enter a Number  :  ");
	scanf("%d",&n);
	if(n>0)
   	printf("Positive number ");
     	else
       	if(n<0)
        	printf("Negative number ");
          	else
            	if(n==0)
             	printf("Number is Zero ");
	return 0;
}
